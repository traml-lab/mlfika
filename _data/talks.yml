# ML Fika talks.
#
# Add one entry per talk. Only `date` and `title` are required; the rest are optional. If `abstract` or `biography` is given, the entry shows an "Abstract & bio" toggle that expands the details. 
#
# Talks automatically move from "Upcoming" to "Past" one hour after their scheduled time, so keep the `date` parseable (e.g. "September 04, 2026 at 3 pm CEST").

# - date: September 01, 2026 at 3 pm CEST
  # title: "TBA"
  # presenter: "TBA"
  # url: https://trappmartin.github.io
  # affiliation: ""
#   abstract: "One or two paragraphs describing the talk. Markdown links work."
#   biography: "A short bio of the speaker."
